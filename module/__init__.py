# Import the following packages in the following order to run the files in the modules
from module.core import *
import tkinter as tk
from tkinter import messagebox, SUNKEN, BOTTOM, X

from module.play_list import MusicPlaylist
from module.core import *

# import the following in order to run the test
import unittest
from unittest import TestCase

from play_list import MusicPlaylist
from module.core import *

# in case of import issues run the below command in terminal and the output will tell you where the modules are being imported from
# python -v -m my_scriptname.py